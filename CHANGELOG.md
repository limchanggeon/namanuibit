# 패치 노트

날짜는 배포한 날입니다. 버전은 [시맨틱 버저닝](https://semver.org/lang/ko/)을 따릅니다.

## 1.1.0 — 2026-09-21

사진 관리와 내보내기를 손봤습니다.

### 새로 생긴 것

- **사진 삭제** — 현상 화면의 휴지통 버튼이나 `⌫` 키로 지웁니다. 라이브러리에서는 여러 장을 골라 한 번에 지울 수 있습니다. 지우기 전에 항상 한 번 묻고, **원본 파일은 건드리지 않습니다.** 라이브러리에 들고 있던 사본과 보정값만 사라집니다.
- **전체 / 선택 내보내기** — 내보내기 창에서 `현재 사진`, `선택한 사진`, `전체 사진` 중에 고릅니다. 여러 장일 때는 저장할 폴더를 묻고, 사진마다 저장해 둔 보정값을 각각 적용합니다. 몇 장을 처리했는지 진행률이 뜨고, 실패한 사진이 있으면 이유와 함께 알려줍니다.
- **자유 영역 자르기** — 도구 막대의 `자르기`(단축키 `C`)를 누르면 사진 위에 크롭 틀이 올라옵니다. 모서리를 끌어 영역을 정하고, 안쪽을 끌어 옮깁니다. 비율을 `자유`로 두면 아무 모양이나 잡을 수 있고, `1:1`·`4:3`·`3:2`·`16:9`·`원본 비율`로 고정할 수도 있습니다. 삼등분선이 같이 보이고, `Enter`로 적용, `Esc`로 취소합니다. 자르기는 언제든 되돌릴 수 있습니다 — 잘라낸 자리는 보정값으로만 저장되고 원본은 그대로입니다.
- **WebP 내보내기** — JPEG과 비슷한 화질에 파일이 더 작습니다. 긴 변 16,383px까지 지원하며, 그보다 큰 사진은 크기를 줄여 내보내라고 알려줍니다.
- **진행 화면** — 여러 장을 내보낼 때 몇 장째인지, 어떤 사진을 처리 중인지, 얼마나 걸렸는지 보여줍니다.

### 고친 것

- **Windows에서 내보내기 중 창이 "응답 없음"이 되던 문제.** 원인은 현상 작업이 창을 그리는 프로세스와 같은 곳에서 돌아, 무거운 계산이 파이썬 인터프리터를 몇 초씩 붙잡고 있었던 것입니다. 그 사이에는 화면이 다시 그려지지 않아서, 로딩 표시를 띄워도 멈춰 보였습니다. 이제 현상은 **별도 프로세스**에서 돕니다. 4,160 × 6,240 사진을 내보내는 동안 창 쪽 프로세스가 멈춰 있는 최대 시간이 210ms에서 14ms로 줄었습니다.

### 알아두실 점

- 이전 버전에서 `1:1` 같은 비율로 잘라 둔 사진은 그대로 유지됩니다. 자르기 도구를 열면 그 영역이 잡혀 있고, 거기서부터 자유롭게 고치면 됩니다.

## 1.0.2 — 2026-09-18

**Windows 배포본이 처음 나왔습니다.** macOS는 달라진 것이 없습니다.

- `windows-latest` 러너가 패키징하고, 만들어진 exe를 실제로 띄워 사진 가져오기부터 내보내기까지 돌려본 뒤 릴리스에 올립니다.
- 윈도우에서만 터지던 것들을 고쳤습니다 — `uvloop`은 윈도우 빌드가 없어 플랫폼 마커를 넣었고, PowerShell 5.1이 BOM 없는 스크립트를 시스템 코드페이지로 읽어 한글이 깨지던 문제와, zip 항목 이름이 역슬래시로 들어가 폴더가 아닌 파일 하나로 풀리던 문제를 잡았습니다.

## 1.0.1 — 2026-09-18

- **휠 확대·축소** — 사진 위에서 휠을 굴리거나 트랙패드로 두 손가락 스크롤하면 확대·축소됩니다. 라이트룸 현상 화면과 같은 동작입니다.
- **트랙패드 핀치** — 두 손가락을 오므리거나 벌리면 그만큼 배율이 바뀝니다.
- **커서 기준 확대** — 보고 있던 지점이 커서 아래 그대로 남습니다. 예전처럼 화면 가운데로 튀지 않습니다.
- 버튼과 `⌘ +` / `⌘ −`는 25 · 33 · 50 · 67 · 100 · 150 · 200 · 300 · 400% 단계로 움직입니다. 휠과 핀치는 그 사이 값도 쓰며 최대 800%까지 갑니다.
- 맞춤 배율보다 작게 축소하면 자동으로 **화면 맞춤**으로 돌아갑니다.

## 1.0.0 — 2026-09-18

첫 배포입니다. 브라우저에서 쓰던 것을 macOS 데스크톱 앱으로 만들었습니다.

- 네이티브 창 안에서 동작하고, 현상은 같은 프로세스 안의 Python 서버가 담당합니다. 사진을 외부로 보내지 않습니다.
- LibRaw/rawpy RAW 디코딩, 노출·색상·HSL·커브·그레이딩·디테일 보정, Camera Raw XMP 프리셋 가져오기.
- 네이티브 저장 대화상자로 JPEG · PNG · TIFF 내보내기 (sRGB ICC 포함).
- 라이브러리는 `~/Library/Application Support/나만의빛/`에 저장됩니다. 원본 파일은 수정하지 않습니다.
